# Corpus collection (load_data.ipynb)
feedparser
requests
beautifulsoup4
newspaper3k
lxml
lxml_html_clean
natasha
yake
googlenewsdecoder
tqdm
setuptools<81

# EDA and graph constructor
networkx>=3.0
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3

# Russian NLP for discourse-graph construction
pymorphy3            # lemmatisation + POS tagging (concept merging / filtering)
pymorphy3-dicts-ru   # Russian dictionaries for pymorphy3
razdel               # sentence/word segmentation
yake                 # keyphrase extraction (multi-word concepts)

# LLM evaluation/reporting (any OpenAI-compatible endpoint: OpenRouter, Ollama, …)
openai>=1.0

# Optional: heavy models (keybert, bertscore, paraphrase_cluster).
# Not needed for the russkaya_istina preset — comment out if not using those vertex methods.
# torch>=2.0
# transformers>=4.40

# Optional: TextRank implementation. Falls back to NetworkX if missing.
# summa
